enum HabitScheduleType {  daily,  weekly,  custom,}
enum PlanType { standard, premium }
enum HabitDifficulty { light,  medium,  hard, }
enum HabitActivityStatus { completed, skipped, }
enum HabitDayStatus { completed, skipped, pending, }
enum HabitDiscoverCategory {
  all,
  mindset,
  wellness,
  fitness,
  health,
  productivity,
  sleep,
  learning,
  beauty,
}